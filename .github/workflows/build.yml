name: Build Windows

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.4"
          channel: "stable"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install "flet>=0.24.0"
          if (Test-Path requirements.txt) { pip install -r requirements.txt }
          pip install static-ffmpeg

      - name: Build Flet App
        env:
          PYTHONIOENCODING: utf-8
        shell: pwsh
        run: |
          # Указываем "." (текущую папку), так как main.py лежит в корне
          Write-Output "y" | flet build windows . --project "Whisper Transcriber" --product "Whisper Transcriber" --org com.whisper

      - name: Zip output
        shell: pwsh
        run: |
          # Flet создаст папку build/windows в корне
          if (Test-Path build\windows) {
            Compress-Archive -Path build\windows\* -DestinationPath WhisperTranscriber-Windows.zip -Force
          } else {
            Write-Error "Build directory not found!"
            exit 1
          }

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: WhisperTranscriber-Windows
          path: WhisperTranscriber-Windows.zip
          if-no-files-found: error
